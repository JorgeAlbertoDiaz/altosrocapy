# Esquema de base de datos — AltosRoca

Diagrama ER de referencia de la base de datos SQLite, generado a partir de la exportación TSV de SQL Server.

La fuente de verdad de este diagrama es [`schema.puml`](./schema.puml).
Para regenerar la imagen tras un cambio en el esquema:

```bash
plantuml -tpng docs/schema.puml -o .
```

![Diagrama ER](./schema.png)
